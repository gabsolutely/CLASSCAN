# detection package
# Submodules: detector, change_trigger, zone_reconciler
# Import explicitly from each submodule — no eager imports here
# so that zone_reconciler and change_trigger can be used without cv2/tflite.
